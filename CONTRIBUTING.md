# Contributing

ORBI uses Conventional Commits and DCO sign-off.

```bash
git checkout -b feat/short-description
git commit -s -m "feat(scope): add concise change"
```

All changes require tests. Security-boundary changes must include an explicit threat-model note.
